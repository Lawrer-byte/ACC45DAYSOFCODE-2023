# cook your dish here
for i in range(int(input())) :
    n,k=map(int,input().split())
    a=list(map(int,input().split()))[:n]
    summ=0
    maxx=0
    for i in range(n-k+1):
        summ= sum(a[i:i+k])
        if(maxx<summ):
            maxx=summ
    print(maxx)
        
